# 巡检任务系统 — 功能方案 v2（多线路支持）

---

## 一、用户角色

| 角色 | 权限概述 |
|------|---------|
| **管理员** | 创建/下发任务（多线路）、查看全部任务、审核任务（通过/驳回） |
| **巡检员** | 查看分配给我的任务、按线路逐条巡检、提交巡检结果 |

---

## 二、核心数据模型（多线路）

```
┌─────────────────────┐
│       Task          │  一条任务
│  id, name, deadline │
│  executor_id        │
│  status (派生)       │
└────────┬────────────┘
         │ 1:N
         ▼
┌─────────────────────┐
│     TaskRoute        │  任务-线路关联（核心巡检单元）
│  id, task_id        │
│  route_id, route_name│
│  status (独立状态)    │  ← 每条线路有独立的巡检状态！
│  inspect_started_at │
│  inspect_completed_at│
│  review_action       │
│  review_reason       │
└────────┬────────────┘
         │ 1:N
         ▼
┌─────────────────────┐
│  InspectionIssue     │  巡检发现的问题
│  id, task_route_id  │
│  image_url, location │
│  description         │
└─────────────────────┘
```

**关键设计原则：巡检状态在"线路级别"而非"任务级别"。任务级别状态为派生字段。**

---

## 三、线路级状态 & 任务级派生

### 3.1 线路级状态（TaskRoute.status）

| 状态值 | 含义 |
|--------|------|
| `PENDING` | 待巡检（该线路尚未开始） |
| `IN_PROGRESS` | 巡检中 |
| `AWAITING_REVIEW` | 巡检完成，待管理员审核 |
| `COMPLETED` | 审核通过 |
| `REJECTED` | 审核驳回 |

### 3.2 任务级状态（Task.status，后端查询时动态派生）

派生规则（从高到低优先级匹配）：

| 优先级 | 判定条件 | 任务状态 |
|--------|---------|---------|
| 1 | **任意一条**线路 `REJECTED` | **已驳回** |
| 2 | 任务 deadline < now() 且不是所有线路都 `COMPLETED` 或 `REJECTED` | **已超时** |
| 3 | **所有**线路都是 `COMPLETED` | **已完成** |
| 4 | **所有**线路都是 `PENDING` | **待巡检** |
| 5 | 存在 `AWAITING_REVIEW` 或 `IN_PROGRESS` 的线路（其余可能已完成/待巡检） | **巡检中**（部分完成） |

> 例：3条线路，线路A 已完成、线路B 待审核、线路C 待巡检 → 任务状态为 **巡检中**

---

## 四、原型图（文字 + 结构描述）

---

### 4.1 任务列表页

```
┌──────────────────────────────────────────────────────────────────┐
│  任务管理                                                         │
│                                                                    │
│  ┌───────────────────────────────────────────────────────────────┐│
│  │ [🔍 任务名称]  [状态筛选 ▼]  [日期范围 📅]  [查询] [重置]    ││
│  │                                             [+ 下发任务]       ││
│  └───────────────────────────────────────────────────────────────┘│
│                                                                    │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ 任务名称      │ 线路        │ 执行人 │ 进度      │ 状态  │ 操作 ││
│  ├────────────────────────────────────────────────────────────────┤│
│  │ 3月管线巡检   │ 1号线       │ 张三   │ 0/3 条    │🔵待巡检│ 详情 ││
│  │               │ 3号线       │        │           │       │      ││
│  │               │ 5号线       │        │           │       │      ││
│  ├────────────────────────────────────────────────────────────────┤│
│  │ 2月设备巡检   │ 2号线       │ 李四   │ 1/2 条    │🟡巡检中│ 详情 ││
│  │               │ 4号线       │        │           │       │      ││
│  ├────────────────────────────────────────────────────────────────┤│
│  │ 1月安全巡检   │ 1号线       │ 王五   │ 3/3 条    │🟠待审核│ 审核 ││
│  │               │ 2号线       │        │           │       │      ││
│  │               │ 3号线       │        │           │       │      ││
│  ├────────────────────────────────────────────────────────────────┤│
│  │ 12月管线巡检  │ 5号线       │ 赵六   │ 2/2 条    │🟢已完成│ 详情 ││
│  │               │ 6号线       │        │           │       │      ││
│  ├────────────────────────────────────────────────────────────────┤│
│  │ 11月设备巡检  │ 2号线       │ 张三   │ 1/2 条    │🔴已驳回│ 详情 ││
│  │               │ 3号线       │        │(线路2驳回)│       │      ││
│  ├────────────────────────────────────────────────────────────────┤│
│  │ 10月安全巡检  │ 1号线       │ 李四   │ 0/1 条    │⚫已超时│ 详情 ││
│  └────────────────────────────────────────────────────────────────┘│
│                                                    [分页: 1 2 3]  │
└──────────────────────────────────────────────────────────────────┘
```

**列表字段变更：**

| 列 | 内容 | 说明 |
|----|------|------|
| 任务名称 | 单行 | 不变 |
| 线路 | **多行展示** | 列出该任务所有线路名，换行或紧凑排列 |
| 执行人 | 单行 | 不变 |
| 进度 | **新增** | `已完成数/总线路数 条`，一目了然 |
| 状态 | Tag | 6种颜色对应派生任务状态 |
| 截止时间 | 单行 | 不变 |
| 操作 | 按钮 | "待审核"状态且当前为管理员时显示 [审核]，其余显示 [详情] |

**状态 Tag 颜色（Element Plus）：**

| 状态 | el-tag type | 效果 |
|------|------------|------|
| 待巡检 | `primary` | 蓝色 |
| 巡检中 | `warning` | 橙色 |
| 待审核 | `warning` | 橙色（加粗或加标记以区分） |
| 已完成 | `success` | 绿色 |
| 已驳回 | `danger` | 红色 |
| 已超时 | `info` | 灰色 |

---

### 4.2 下发任务弹窗（管理员）

```
┌───────────────────────────────────────────────┐
│  下发巡检任务                          [X]    │
├───────────────────────────────────────────────┤
│                                               │
│  任务名称   [___________________________]     │
│                                               │
│  巡检线路   [▼ 请选择线路（可多选）      ]    │
│             ┌─────────────────────────┐       │
│             │ ☑ 1号线                  │       │
│             │ ☑ 2号线                  │       │
│             │ ☐ 3号线                  │       │
│             │ ☑ 5号线                  │       │
│             └─────────────────────────┘       │
│             已选: 1号线, 2号线, 5号线 (共3条)  │
│                                               │
│  截止日期   [📅 选择日期               ]     │
│             (最多选未来7天)                    │
│                                               │
│  执行人     [▼ 请选择巡检员            ]     │
│             (单选，请求 /api/users?role=inspector)│
│                                               │
│             [取消]         [确认下发]          │
└───────────────────────────────────────────────┘
```

**变更要点：**
- 线路选择从单选改为**多选**（`el-select` 开启 `multiple`，或用 `el-checkbox-group`）
- 已选线路标签可删除（`el-tag` + closable）
- 至少选 1 条线路，否则 [确认下发] 置灰

---

### 4.3 任务详情页（核心变化：按线路展示）

**采用 Drawer 抽屉或独立页面，按线路分段展示每条线路的巡检情况：**

```
┌──────────────────────────────────────────────────────────────┐
│  任务详情                                            [X]     │
├──────────────────────────────────────────────────────────────┤
│  基本信息                                                    │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 任务名称：3月管线巡检                                   │  │
│  │ 区域ID：330105                                           │  │
│  │ 截止日期：2026-03-20 18:00                             │  │
│  │ 创建时间：2026-03-13 14:30                             │  │
│  │ 整体状态：🟡 巡检中（1/3 条线路已完成）                  │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  线路巡检明细                                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 📍 1号线                         状态：🟢 已完成       │  │
│  │    巡检时间：03-14 09:00 ~ 09:45                       │  │
│  │    ✅ 无问题    [查看详情]                              │  │
│  ├────────────────────────────────────────────────────────┤  │
│  │ 📍 3号线                         状态：🟠 待审核       │  │
│  │    巡检完成时间：03-14 15:20                            │  │
│  │    ⚠️ 发现问题 2 处    [查看详情]                       │  │
│  ├────────────────────────────────────────────────────────┤  │
│  │ 📍 5号线                         状态：🔵 待巡检       │  │
│  │    尚未开始                                            │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│                                         [关闭]                │
└──────────────────────────────────────────────────────────────┘
```

---

### 4.4 审核视图（管理员点击"审核"或线路待审核时展开）

```
┌──────────────────────────────────────────────────────────────┐
│  任务详情 — 审核                                     [X]     │
├──────────────────────────────────────────────────────────────┤
│  基本信息（同上，略）                                        │
│                                                              │
│  ═══════════ 待审核线路：3号线 ═══════════                    │
│                                                              │
│  巡检结果                                                    │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 问题 1 / 2                                              │  │
│  │ ┌──────────┐                                           │  │
│  │ │          │  ← 点击可 el-image-viewer 大图预览         │  │
│  │ │  图片1   │                                           │  │
│  │ └──────────┘                                           │  │
│  │ 位置：3号线 K5+200 标段                                 │  │
│  │ 描述：管道外壁有明显锈蚀痕迹，面积约20cm²                 │  │
│  │ 巡检人：张三    时间：03-14 15:30                       │  │
│  │ ──────────────────────────────────────                  │  │
│  │ 问题 2 / 2                                              │  │
│  │ ┌──────────┐                                           │  │
│  │ │  图片2   │                                           │  │
│  │ └──────────┘                                           │  │
│  │ 位置：3号线 K5+800 标段                                 │  │
│  │ 描述：标识牌倾斜约30度                                   │  │
│  │ 巡检人：张三    时间：03-14 16:00                       │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  审核操作（针对该线路）                                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  ○ 通过该线路    ○ 驳回该线路                          │  │
│  │                                                        │  │
│  │ 驳回原因：（选中"驳回"时显示，必填，max 200字）          │  │
│  │ ┌──────────────────────────────────────────────────┐   │  │
│  │ │                                                  │   │  │
│  │ └──────────────────────────────────────────────────┘   │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ═══════════ 待审核线路：5号线 ═══════════                    │
│  （同上结构...）                                             │
│                                                              │
│  ───────────────────────────────────────────────             │
│  批量操作（可选）：                                          │
│  [全部通过]  [全部驳回并填写统一原因...]                     │
│                                                              │
│                        [取消]    [提交审核]                   │
└──────────────────────────────────────────────────────────────┘
```

**审核交互要点：**
| 规则 | 说明 |
|------|------|
| 审核粒度 | **按线路审核**，每条待审核线路独立决定通过/驳回 |
| 驳回原因 | 每条线路独立填写驳回原因（textarea） |
| 批量操作 | 提供"全部通过""全部驳回"快捷按钮（可选） |
| 提交 | 一次性提交所有线路的审核结果 |
| 无问题线路 | 巡检结果区显示 "✅ 本次巡检未发现问题" |
| 已驳回线路 | 巡检员可针对该线路修改并重新提交 |

---

## 五、后端 API 列表

### 5.1 基础数据

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| `GET` | `/api/v1/routes` | 获取所有线路列表 | 管理员 |
| `GET` | `/api/v1/users/executors` | 获取巡检员列表 | 管理员 |

### 5.2 任务 CRUD

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| `GET` | `/api/v1/tasks` | 任务列表（分页+筛选） | 全员 |
| `GET` | `/api/v1/tasks/{id}` | 任务详情（含所有线路状态） | 全员 |
| `POST` | `/api/v1/tasks` | 下发任务（多线路） | 管理员 |

### 5.3 线路级巡检操作

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| `PUT` | `/api/v1/task-routes/{routeId}/start` | 开始巡检某条线路 | 巡检员 |
| `POST` | `/api/v1/task-routes/{routeId}/submit` | 提交某条线路巡检结果 | 巡检员 |

### 5.4 线路级审核

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| `PUT` | `/api/v1/task-routes/{routeId}/review` | 审核某条线路 | 管理员 |
| `PUT` | `/api/v1/tasks/{id}/batch-review` | 批量审核（可选） | 管理员 |

### 5.5 巡检问题

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| `GET` | `/api/v1/task-routes/{routeId}/issues` | 获取某线路的巡检问题 | 全员 |
| `POST` | `/api/v1/task-routes/{routeId}/issues` | 添加巡检问题（含图片） | 巡检员 |

### 5.6 接口汇总

| 序号 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 1 | `GET` | `/api/v1/routes` | 线路列表 |
| 2 | `GET` | `/api/v1/users/executors` | 巡检员列表 |
| 3 | `GET` | `/api/v1/tasks` | 任务列表 |
| 4 | `GET` | `/api/v1/tasks/{id}` | 任务详情 |
| 5 | `POST` | `/api/v1/tasks` | 下发任务 |
| 6 | `PUT` | `/api/v1/task-routes/{id}/start` | 开始巡检线路 |
| 7 | `POST` | `/api/v1/task-routes/{id}/submit` | 提交线路巡检 |
| 8 | `PUT` | `/api/v1/task-routes/{id}/review` | 审核线路 |
| 9 | `GET` | `/api/v1/task-routes/{id}/issues` | 获取线路问题 |
| 10 | `POST` | `/api/v1/task-routes/{id}/issues` | 添加线路问题 |

---

## 六、API 请求/响应示例

### 6.1 下发任务 POST /api/v1/tasks

```json
// Request
{
  "taskName": "3月管线巡检",
  "routeIds": [101, 103, 105],
  "deadline": "2026-03-20T18:00:00",
  "executorId": 10
}

// Response
{
  "code": 0,
  "data": {
    "taskId": 42,
    "taskRoutes": [
      { "id": 201, "routeId": 101, "routeName": "1号线", "status": "PENDING" },
      { "id": 202, "routeId": 103, "routeName": "3号线", "status": "PENDING" },
      { "id": 203, "routeId": 105, "routeName": "5号线", "status": "PENDING" }
    ]
  },
  "message": "任务下发成功，共3条线路"
}
```

### 6.2 任务列表 GET /api/v1/tasks

```json
// Response
{
  "code": 0,
  "data": {
    "total": 56,
    "page": 1,
    "pageSize": 10,
    "list": [
      {
        "id": 42,
        "taskName": "3月管线巡检",
        "executorId": 10,
        "executorName": "张三",
        "deadline": "2026-03-20T18:00:00",
        "status": "IN_PROGRESS",
        "routeCount": 3,
        "completedRouteCount": 1,
        "routes": [
          { "routeId": 101, "routeName": "1号线", "status": "COMPLETED" },
          { "routeId": 103, "routeName": "3号线", "status": "AWAITING_REVIEW" },
          { "routeId": 105, "routeName": "5号线", "status": "PENDING" }
        ],
        "createdAt": "2026-03-13T14:30:00"
      }
    ]
  }
}
```

### 6.3 任务详情 GET /api/v1/tasks/{id}

```json
{
  "code": 0,
  "data": {
    "id": 42,
    "taskName": "3月管线巡检",
    "executorId": 10,
    "executorName": "张三",
    "creatorId": 1,
    "creatorName": "管理员",
    "deadline": "2026-03-20T18:00:00",
    "status": "IN_PROGRESS",
    "routeCount": 3,
    "completedRouteCount": 1,
    "createdAt": "2026-03-13T14:30:00",
    "taskRoutes": [
      {
        "id": 201,
        "routeId": 101,
        "routeName": "1号线",
        "status": "COMPLETED",
        "inspectStartedAt": "2026-03-14T09:00:00",
        "inspectCompletedAt": "2026-03-14T09:45:00",
        "hasProblems": false,
        "issueCount": 0,
        "reviewAction": "APPROVED",
        "reviewReason": null
      },
      {
        "id": 202,
        "routeId": 103,
        "routeName": "3号线",
        "status": "AWAITING_REVIEW",
        "inspectStartedAt": "2026-03-14T14:00:00",
        "inspectCompletedAt": "2026-03-14T15:20:00",
        "hasProblems": true,
        "issueCount": 2,
        "reviewAction": null,
        "reviewReason": null
      },
      {
        "id": 203,
        "routeId": 105,
        "routeName": "5号线",
        "status": "PENDING",
        "inspectStartedAt": null,
        "inspectCompletedAt": null,
        "hasProblems": false,
        "issueCount": 0,
        "reviewAction": null,
        "reviewReason": null
      }
    ]
  }
}
```

### 6.4 提交线路巡检 POST /api/v1/task-routes/{id}/submit

```json
// Request (multipart/form-data 或分步上传)
{
  "hasProblems": true,
  "issues": [
    {
      "imageUrl": "https://cdn.xxx/img1.jpg",
      "location": "3号线 K5+200 标段",
      "description": "管道外壁有明显锈蚀痕迹，面积约20cm²"
    },
    {
      "imageUrl": "https://cdn.xxx/img2.jpg",
      "location": "3号线 K5+800 标段",
      "description": "标识牌倾斜约30度"
    }
  ]
}

// Response
{
  "code": 0,
  "message": "线路巡检提交成功，状态已更新为待审核"
}
```

### 6.5 审核线路 PUT /api/v1/task-routes/{id}/review

```json
// Request
{
  "action": "REJECT",
  "reason": "问题图片不够清晰，请重新拍摄后提交"
}
// action 取值: APPROVE | REJECT
// REJECT 时 reason 必填，1~500 字符

// Response
{
  "code": 0,
  "message": "线路审核已驳回"
}
```

---

## 七、数据库表设计

### tasks 任务表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| task_name | varchar(100) | 任务名称 |
| executor_id | bigint | 执行人ID |
| executor_name | varchar(50) | 冗余执行人姓名 |
| creator_id | bigint | 创建人ID |
| deadline | datetime | 截止日期 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

> 任务表不再存储 status，状态由 task_routes 派生。

### task_routes 任务-线路关联表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| task_id | bigint FK | 关联任务ID |
| route_id | bigint | 线路ID |
| route_name | varchar(100) | 冗余线路名称 |
| status | varchar(20) | PENDING / IN_PROGRESS / AWAITING_REVIEW / COMPLETED / REJECTED |
| inspect_started_at | datetime | 开始巡检时间 |
| inspect_completed_at | datetime | 巡检完成时间 |
| review_action | varchar(10) | APPROVE / REJECT |
| review_reason | varchar(500) | 驳回原因 |
| reviewer_id | bigint | 审核人ID |
| reviewed_at | datetime | 审核时间 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### inspection_issues 巡检问题表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| task_route_id | bigint FK | 关联 task_routes.id |
| image_url | varchar(500) | 问题图片URL |
| location | varchar(200) | 问题位置描述 |
| description | varchar(1000) | 问题详细描述 |
| created_by | bigint | 提交人ID |
| created_at | datetime | 提交时间 |

---

## 八、任务状态派生 SQL 思路

```sql
-- 查询任务列表时，用子查询派生任务状态
SELECT t.*,
  (
    SELECT 
      CASE
        WHEN SUM(CASE WHEN tr.status = 'REJECTED' THEN 1 ELSE 0 END) > 0 
          THEN 'REJECTED'
        WHEN t.deadline < NOW() 
          AND SUM(CASE WHEN tr.status NOT IN ('COMPLETED','REJECTED') THEN 1 ELSE 0 END) > 0
          THEN 'TIMEOUT'
        WHEN SUM(CASE WHEN tr.status = 'COMPLETED' THEN 1 ELSE 0 END) = COUNT(*)
          THEN 'COMPLETED'
        WHEN SUM(CASE WHEN tr.status = 'PENDING' THEN 1 ELSE 0 END) = COUNT(*)
          THEN 'PENDING'
        ELSE 'IN_PROGRESS'
      END
    FROM task_routes tr WHERE tr.task_id = t.id
  ) AS status,
  (SELECT COUNT(*) FROM task_routes tr WHERE tr.task_id = t.id) AS route_count,
  (SELECT COUNT(*) FROM task_routes tr WHERE tr.task_id = t.id AND tr.status = 'COMPLETED') AS completed_route_count
FROM tasks t
```

---

## 九、前端路由 & 组件树

### 路由

```
/tasks              → 任务列表页
/tasks/:id          → 任务详情页（含审核功能）
```

### 组件树（Element Plus）

```
TaskListPage.vue
├── 搜索筛选栏
│   ├── el-input (任务名称搜索)
│   ├── el-select (状态筛选)
│   ├── el-date-picker (日期范围)
│   ├── el-button [查询] [重置]
│   └── el-button [+ 下发任务] (管理员可见)
├── el-table
│   ├── 列：任务名称
│   ├── 列：线路（多行展示，紧凑排列）
│   ├── 列：执行人
│   ├── 列：进度（completedRouteCount / routeCount 条）
│   ├── 列：状态（el-tag，6色）
│   ├── 列：截止时间
│   └── 列：操作 [查看详情] / [审核]
├── el-pagination
│
├── TaskCreateDialog.vue (下发任务弹窗)
│   ├── el-form
│   │   ├── el-input (任务名称，必填)
│   │   ├── el-select multiple (巡检线路多选，必填至少1条)
│   │   ├── el-date-picker (截止日期，限制7天)
│   │   └── el-select (执行人单选，必填)
│   └── el-button [确认下发]
│
└── TaskDetailDrawer.vue (详情抽屉)
    ├── el-descriptions (基本信息)
    ├── 线路巡检明细 (v-for taskRoutes)
    │   ├── 线路名称 + el-tag (线路状态)
    │   ├── 巡检时间
    │   ├── 问题概况（有无问题、问题数）
    │   └── el-button [查看详情] → 展开该线路问题列表
    ├── 审核操作区 (v-if 管理员 && 存在待审核线路)
    │   ├── 每条待审核线路的问题详情
    │   │   ├── el-image (可预览)
    │   │   ├── 位置、描述文本
    │   │   └── el-divider (问题之间分隔)
    │   ├── el-radio-group (通过/驳回)
    │   ├── el-input type=textarea (驳回原因，条件显示)
    │   ├── [全部通过] [全部驳回] 快捷按钮（可选）
    │   └── el-button [提交审核]
    └── el-button [关闭]
```

---

## 十、移动端原型图（地图化设计，参考高德/谷歌地图）

移动端核心思路：**地图即主界面**，所有操作围绕地图展开。使用 H5 + 高德地图 JSAPI（或对应地图 SDK）。

### 10.1 页面状态总览

```
移动端共 8 个 UI 状态，全部在同一地图页面内切换：

[空闲地图] → [任务列表半窗] → [线路绘制] → [线路详情弹出]
                                              ↓
                                        [到达提醒] → [巡检模式] ⇄ [上报问题]
                                              ↓
                                        [巡检完成] → [提交任务]
```

---

### 10.2 状态一：空闲地图（首页默认）

```
┌──────────────────────────────────────┐
│  ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔   │  ← 状态栏
│  ┌────┐                             │
│  │ ☰  │         地图画布             │  ← 左上菜单
│  └────┘                             │
│          🟦 (当前定位蓝点)            │
│                                     │
│                                     │
│                    ┌──────────────┐  │
│                    │  🔍 巡检任务  │  │  ← 悬浮操作按钮
│                    └──────────────┘  │
│                                     │
│  ─────────────────────────────────  │
│  📍 北京市朝阳区...                  │  ← 底部定位信息条（可选）
└──────────────────────────────────────┘
```

**交互：**
- 双指缩放、单指拖动地图
- 蓝色定位点跟随用户实时位置
- 点击 [🔍 巡检任务] → 弹出任务列表半窗（状态二）

---

### 10.3 状态二：任务列表半窗

```
┌──────────────────────────────────────┐
│         地图画布（半透明遮罩）         │
│  ┌──────────────────────────────────┐│
│  │  我的巡检任务              [X]   ││  ← 标题栏
│  │  ─────────────────────────────── ││
│  │  ┌──────────────────────────────┐││
│  │  │ ● 1月管线巡检               │││
│  │  │   线路：1号线、2号线、3号线    │││  ← 任务卡片
│  │  │   进度：1/3 条  截止：01-20   │││
│  │  │   🟡 巡检中               > │││
│  │  └──────────────────────────────┘││
│  │  ┌──────────────────────────────┐││
│  │  │ ● 12月安全巡检              │││
│  │  │   线路：4号线、5号线          │││
│  │  │   进度：0/2 条  截止：01-19   │││
│  │  │   🔵 待巡检               > │││
│  │  └──────────────────────────────┘││
│  │  ┌──────────────────────────────┐││
│  │  │ ● 11月设备巡检              │││
│  │  │   线路：8号线                │││
│  │  │   进度：1/1 条  截止：01-15   │││
│  │  │   🟠 待审核               > │││
│  │  └──────────────────────────────┘││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

**交互：**
- 半窗占屏幕 60%，可上拉至全屏
- 按截止日期排序，最近截止在上
- 点击卡片 → 关闭半窗 → 进入状态三（线路绘制）

---

### 10.4 状态三：线路绘制 + 任务信息

点选任务后，地图上绘制该任务所有线路：

```
┌──────────────────────────────────────┐
│         地图画布                      │
│                                     │
│   🔵━━━━━━━━━━━━🔵   ← 蓝线=未巡检    │
│   🟠━━━━━━━━━━━━🟠   ← 橙线=巡检中    │
│   🟢━━━━━━━━━━━━🟢   ← 绿线=已完成    │
│                                     │
│  ┌──────────────────────────────────┐│
│  │  1月管线巡检            [X]      ││  ← 底部任务信息卡
│  │  共3条线路 · 巡检中               ││
│  │  ┌──────────────────────────────┐││
│  │  │ 🔵 1号线  待巡检    [开始巡检]│││
│  │  │ 🟠 2号线  巡检中    [继续巡检]│││
│  │  │ 🟢 3号线  已完成    [查看详情]│││
│  │  └──────────────────────────────┘││
│  │                          [提交任务]││  ← 全部完成才亮起
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

**颜色约定：**
| 颜色 | 线路状态 |
|------|---------|
| 🔵 蓝色 `#1677FF` | 待巡检 PENDING |
| 🟠 橙色 `#FA8C16` | 巡检中 IN_PROGRESS |
| 🟢 绿色 `#52C41A` | 已完成 COMPLETED |
| 🔴 红色 `#FF4D4F` | 已驳回 REJECTED |

**交互：**
- 已完成的线路默认显示，可在图例中切换隐藏
- 点击某条线路的色带 → 进入状态四（线路详情弹出）
- "开始巡检" / "继续巡检" 按钮判定用户是否在线路附近
  - 在附近（≤100m）→ 直接进入巡检模式（状态六）
  - 不在附近 → 先弹出导航引导（状态四）

---

### 10.5 状态四：线路详情弹出层

点击地图上的线路后弹出：

```
┌──────────────────────────────────────┐
│         地图画布（半透明遮罩）         │
│                                     │
│  ┌──────────────────────────────────┐│
│  │  1号线                    [X]    ││
│  │  ─────────────────────────────── ││
│  │  状态：🔵 待巡检                  ││
│  │  长度：约 3.2 公里                ││
│  │  起点：朝阳区建国路100号           ││
│  │  终点：通州区新华大街200号         ││
│  │                                  ││
│  │  ┌──────────────────────────────┐││
│  │  │     🧭 导航到这里            │││  ← 唤起高德/谷歌地图导航
│  │  └──────────────────────────────┘││
│  │  ┌──────────────────────────────┐││
│  │  │  📍 距离起点约 3.5 公里       │││  ← 实时距离计算
│  │  │  (到达附近后可开始巡检)        │││
│  │  └──────────────────────────────┘││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

**交互：**
- [🧭 导航到这里] → 调用地图 SDK 的导航能力，跳转高德/谷歌地图 App 或 H5 导航
- 距离计算：每 5 秒用当前 GPS 和线路起点计算直线距离
- 当距离 ≤ 100m 时，自动弹出"到达提醒"（状态五）

---

### 10.6 状态五：到达提醒

```
┌──────────────────────────────────────┐
│         地图画布                      │
│                                     │
│  ┌──────────────────────────────────┐│
│  │  📍 您已到达 1号线 巡检位置       ││  ← 顶部浮动通知（从上方滑入）
│  │     距线路起点约 80 米            ││
│  │  ┌──────────────────────────────┐││
│  │  │     ▶ 开始巡检              │││
│  │  └──────────────────────────────┘││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

**触发条件：**
- 用户 GPS 位置与待巡检线路任一端点 ≤ 100 米
- 或用户 GPS 位置与线路中心线最近点 ≤ 50 米
- 每条线路每个巡检周期只提醒一次

**交互：**
- [▶ 开始巡检] → 进入巡检模式（状态六）
- 通知条 5 秒后自动收起（但不影响按钮）
- 如果用户划走通知，仍可在线路信息卡中手动点击 [开始巡检]

---

### 10.7 状态六：巡检模式（核心）

巡检模式的地图界面：

```
┌──────────────────────────────────────┐
│  ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔   │
│  ┌──────────────────────┐           │
│  │ 🟠 巡检中 · 1号线     │           │  ← 顶部状态条
│  │ 已记录 2 个问题 · 32分钟           │
│  └──────────────────────┘           │
│                                     │
│   🔵━━━━━━━━━━━━━━━━━━🔵  ← 预设线路  │
│   🔴━━━━━━🔴            ← 实时巡检轨迹│
│        🟦               ← 当前位置   │
│          ❗                           │
│            ❗       ← 问题标记点       │
│                                     │
│                    ┌────┐            │
│                    │ 📷 │  ← 拍照上报 │
│                    └────┘            │
│  ┌──────────────────────────────┐   │
│  │  🟢 完成巡检                  │   │  ← 底部完成按钮
│  └──────────────────────────────┘   │
└──────────────────────────────────────┘
```

**巡检模式核心功能：**

| 功能 | 描述 |
|------|------|
| 实时轨迹 | GPS 每 3~5 秒打点，红色折线实时绘制巡检路径 |
| 预设线路 | 蓝色线路始终显示作为参考 |
| 拍照上报 | 点击 📷 → 调起相机拍照 → 自动附带经纬度 → 进入问题编辑 |
| 问题标记 | 已上报的问题在地图上以红色 ❗ 标记 |
| 状态条 | 顶部实时显示：当前线路、问题数、巡检时长 |
| 完成按钮 | 随时可点击 [完成巡检] → 弹出确认 → 结束本条线路巡检 |

---

### 10.8 状态七：上报问题

```
┌──────────────────────────────────────┐
│         地图画布（半透明遮罩）         │
│  ┌──────────────────────────────────┐│
│  │  上报问题                  [X]   ││
│  │  ─────────────────────────────── ││
│  │                                  ││
│  │  ┌──────────────────────────────┐││
│  │  │                              │││
│  │  │        [问题照片]             │││  ← 拍照预览
│  │  │        (可重拍)               │││
│  │  └──────────────────────────────┘││
│  │                                  ││
│  │  📍 经纬度                        ││
│  │  116.397428, 39.908722           ││  ← 自动获取，可手动修正
│  │                                  ││
│  │  📝 问题描述                      ││
│  │  ┌──────────────────────────────┐││
│  │  │ 电箱门未关闭，存在安全隐患     │││
│  │  │                              │││
│  │  └──────────────────────────────┘││
│  │                                  ││
│  │  📌 位置描述                      ││
│  │  ┌──────────────────────────────┐││
│  │  │ 1号线 K12+300 标段，5号电杆旁  │││  ← 可逆地理编码预填
│  │  └──────────────────────────────┘││
│  │                                  ││
│  │  ┌──────────────────────────────┐││
│  │  │       确认上报               │││
│  │  └──────────────────────────────┘││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

**交互：**
- 拍照自动触发，照片即时预览
- 经纬度自动获取当前 GPS
- 位置描述可调用逆地理编码（高德 `regeocode`）预填地址
- 确认后问题标记点到地图，可继续巡检

---

### 10.9 状态八：线路巡检完成 & 提交任务

**单条线路完成时：**
```
┌──────────────────────────────────────┐
│  ┌──────────────────────────────┐    │
│  │  ✅ 1号线巡检完成             │    │  ← Toast 提示
│  │  共发现 2 个问题              │    │
│  └──────────────────────────────┘    │
│                                      │
│         地图画布                      │
│   🟢━━━━━━━━━━━━━━🟢   ← 该线路变绿  │
│                                      │
│  ┌──────────────────────────────────┐│
│  │  1月管线巡检                     ││
│  │  🟢 1号线 已完成    [查看详情]    ││
│  │  🔵 2号线 待巡检    [导航前往]    ││  ← 自动引导下一条
│  │  🟢 3号线 已完成    [查看详情]    ││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

**全部线路完成时：**
```
┌──────────────────────────────────────┐
│         地图画布                      │
│                                      │
│  ┌──────────────────────────────────┐│
│  │  🎉 全部巡检完成！               ││
│  │  共巡检 3 条线路，发现问题 5 处    ││
│  │                                  ││
│  │  ┌──────────────────────────────┐││
│  │  │     📤 提交任务              │││  ← 醒目按钮
│  │  └──────────────────────────────┘││
│  │                                  ││
│  │  提交后将由管理员审核             ││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

---

### 10.10 移动端组件树

```
MobileInspectionPage.vue (地图主页面)
├── MapView (高德/谷歌地图 SDK 封装)
│   ├── 用户定位蓝点 (GPS 实时)
│   ├── 线路折线层 (Polyline, 按状态着色)
│   ├── 巡检轨迹层 (Polyline, 红色, 实时追加)
│   └── 问题标记层 (Marker, 点击查看详情)
│
├── FloatingActionButton (悬浮巡检任务按钮)
│
├── TaskListSheet.vue (任务列表半窗)
│   └── 任务卡片列表 (v-for, 点击选中)
│
├── RouteInfoPopup.vue (线路详情弹出层)
│   ├── 线路基本信息
│   ├── 导航按钮 → 唤起第三方导航
│   └── 距离实时显示
│
├── ArrivalBanner.vue (到达提醒横幅)
│   └── 开始巡检按钮
│
├── InspectionStatusBar.vue (巡检中顶部状态条)
│   ├── 当前线路名称
│   ├── 问题计数
│   └── 巡检计时
│
├── CameraButton.vue (悬浮拍照按钮)
│   └── 调起相机 → 自动定位
│
├── IssueReportSheet.vue (问题上报半窗)
│   ├── 照片预览
│   ├── 经纬度 (自动 + 手动)
│   ├── 问题描述 (textarea)
│   ├── 位置描述 (逆地理编码预填)
│   └── 确认上报按钮
│
├── CompleteConfirmDialog.vue (完成确认弹窗)
│
└── SubmitTaskDialog.vue (提交任务弹窗)
    ├── 巡检汇总
    └── 确认提交按钮
```

---

## 十一、PC 端巡检路径回放

管理员在 PC 端任务详情中可查看巡检员的实际巡检轨迹。

### 11.1 路径回放区域

在任务详情抽屉中，对于已巡检的线路，增加路径展示：

```
┌──────────────────────────────────────┐
│  任务详情 — 1月管线巡检               │
│  ─────────────────────────────────── │
│  基本信息区域...                      │
│                                      │
│  线路巡检明细                         │
│  ┌──────────────────────────────────┐│
│  │ 🟢 1号线  已完成  03-14 14:30    ││
│  │                                  ││
│  │ ┌──────────────────────────────┐ ││
│  │ │                              │ ││
│  │ │      [迷你地图画布]           │ ││  ← PC 端地图组件
│  │ │   🔵━━━━━━━━🔵 预设线路      │ ││
│  │ │   🔴━━━━🔴     巡检轨迹      │ ││
│  │ │   ❗  ❗        问题标记      │ ││
│  │ │                              │ ││
│  │ └──────────────────────────────┘ ││
│  │                                  ││
│  │  巡检统计                         ││
│  │  ┌──────────────────────────────┐ ││
│  │  │ 巡检时长：32 分钟            │ ││
│  │  │ 巡检里程：2.8 公里           │ ││
│  │  │ 轨迹点数：384 个             │ ││
│  │  │ 发现问题：2 处               │ ││
│  │  │ 开始时间：03-14 14:02        │ ││
│  │  │ 结束时间：03-14 14:34        │ ││
│  │  └──────────────────────────────┘ ││
│  │                                  ││
│  │  巡检轨迹明细（可折叠）            ││
│  │  ┌──────────────────────────────┐ ││
│  │  │ 03-14 14:02:15  进入线路起点  │ ││
│  │  │ 03-14 14:05:30  K12+100      │ ││
│  │  │ 03-14 14:08:12  K12+200      │ ││
│  │  │ 03-14 14:11:45  K12+300      │ ││
│  │  │ 03-14 14:11:50  ❗ 上报问题   │ ││
│  │  │ ...                         │ ││
│  │  └──────────────────────────────┘ ││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

### 11.2 PC 端地图选型

- 推荐使用高德地图 JSAPI 2.0
- 迷你地图尺寸约 600×300px，不可拖动（只读查看）
- 问题标记点可点击弹出问题照片和描述浮窗
- 可选：[播放轨迹] 按钮，动画回放巡检路径

### 11.3 路径回放组件

```
TrackViewer.vue (PC 端路径回放组件)
├── 迷你地图容器 (非交互/只读)
│   ├── 预设线路 Polyline (蓝色虚线)
│   ├── 巡检轨迹 Polyline (红色实线)
│   └── 问题标记 Marker (红色 ❗，可点击)
├── 巡检统计面板
│   ├── 时长、里程、点数、问题数
│   └── 开始/结束时间
├── 轨迹时间轴 (可折叠)
│   └── Timeline + 问题事件高亮
└── [播放轨迹] 按钮 (可选功能)
```

---

## 十二、移动端 & 路径回放 API 补充

### 12.1 API 汇总（新增 6 个）

| 序号 | 方法 | 路径 | 说明 | 使用场景 |
|------|------|------|------|---------|
| A1 | `GET` | `/api/v1/tasks/my` | 我的巡检任务列表 | 移动端任务半窗 |
| A2 | `GET` | `/api/v1/routes/{id}/coordinates` | 获取线路坐标点（LineString） | 地图绘制线路 |
| A3 | `GET` | `/api/v1/task-routes/{routeId}/tracks` | 获取巡检轨迹点列表 | PC端路径回放 |
| A4 | `POST` | `/api/v1/task-routes/{routeId}/tracks` | 上报单个轨迹点 | 移动端实时打点 |
| A5 | `POST` | `/api/v1/task-routes/{routeId}/tracks/batch` | 批量上报轨迹点 | 弱网时批量上传 |
| A6 | `PUT` | `/api/v1/task-routes/{routeId}/arrive` | 标记到达巡检位置 | 到达提醒记录 |

### 12.2 API 请求/响应示例

**A1: GET /api/v1/tasks/my**
```
请求: GET /api/v1/tasks/my?status=PENDING,IN_PROGRESS
响应:
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "taskName": "1月管线巡检",
      "deadline": "2026-01-20T18:00:00",
      "status": "IN_PROGRESS",
      "routeCount": 3,
      "completedRouteCount": 1,
      "routes": [
        { "id": 101, "routeName": "1号线", "status": "PENDING" },
        { "id": 102, "routeName": "2号线", "status": "IN_PROGRESS" },
        { "id": 103, "routeName": "3号线", "status": "COMPLETED" }
      ]
    }
  ]
}
```

**A2: GET /api/v1/routes/{id}/coordinates**
```
响应:
{
  "code": 200,
  "data": {
    "routeId": 101,
    "routeName": "1号线",
    "coordinates": [
      { "lng": 116.397, "lat": 39.908 },
      { "lng": 116.401, "lat": 39.910 },
      { "lng": 116.408, "lat": 39.913 }
    ]
  }
}
```

**A3: GET /api/v1/task-routes/{routeId}/tracks**
```
响应:
{
  "code": 200,
  "data": {
    "routeId": 101,
    "taskRouteId": 201,
    "stats": {
      "durationMinutes": 32,
      "distanceKm": 2.8,
      "pointCount": 384,
      "issueCount": 2,
      "startedAt": "2026-03-14T14:02:15",
      "endedAt": "2026-03-14T14:34:00"
    },
    "points": [
      { "lng": 116.3971, "lat": 39.9081, "recordedAt": "2026-03-14T14:02:15" },
      { "lng": 116.3975, "lat": 39.9083, "recordedAt": "2026-03-14T14:02:20" },
      { "lng": 116.3980, "lat": 39.9086, "recordedAt": "2026-03-14T14:02:25" }
    ]
  }
}
```

**A4: POST /api/v1/task-routes/{routeId}/tracks**
```
请求体:
{
  "lng": 116.3975,
  "lat": 39.9083,
  "altitude": 45.2,
  "accuracy": 5.0,
  "recordedAt": "2026-03-14T14:02:20"
}
```

**A5: POST /api/v1/task-routes/{routeId}/tracks/batch**
```
请求体:
{
  "points": [
    { "lng": 116.3975, "lat": 39.9083, "recordedAt": "2026-03-14T14:02:20" },
    { "lng": 116.3978, "lat": 39.9085, "recordedAt": "2026-03-14T14:02:25" },
    { "lng": 116.3981, "lat": 39.9087, "recordedAt": "2026-03-14T14:02:30" }
  ]
}
```

### 12.3 新增数据库表

```sql
-- 巡检轨迹点表
CREATE TABLE inspection_tracks (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  task_route_id BIGINT NOT NULL COMMENT '关联 task_routes.id',
  lng DECIMAL(10, 7) NOT NULL COMMENT '经度',
  lat DECIMAL(10, 7) NOT NULL COMMENT '纬度',
  altitude DECIMAL(8, 2) COMMENT '海拔(米)',
  accuracy DECIMAL(6, 2) COMMENT '定位精度(米)',
  recorded_at DATETIME NOT NULL COMMENT '轨迹点采集时间',
  created_at DATETIME DEFAULT NOW(),
  INDEX idx_task_route (task_route_id),
  INDEX idx_recorded_at (task_route_id, recorded_at)
) COMMENT '巡检轨迹记录';

-- 线路坐标表（扩充 routes 表）
-- 如果 routes 表已有坐标字段则无需新建
ALTER TABLE routes ADD COLUMN coordinates JSON COMMENT '线路坐标点 [{lng, lat}, ...]';
```

### 12.4 更新 inspection_issues 表

巡检问题挂到具体线路，且带上经纬度：

```sql
ALTER TABLE inspection_issues 
  ADD COLUMN task_route_id BIGINT NOT NULL COMMENT '关联 task_routes.id',
  ADD COLUMN lng DECIMAL(10,7) COMMENT '问题经度',
  ADD COLUMN lat DECIMAL(10,7) COMMENT '问题纬度',
  ADD INDEX idx_task_route (task_route_id);
```

---

## 十三、完整接口汇总表（含移动端）

| 序号 | 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|------|
| 1 | `GET` | `/api/v1/routes` | 线路列表（下拉用） | 管理员 |
| 2 | `GET` | `/api/v1/routes/{id}/coordinates` | 线路坐标（地图绘制） | 巡检员/管理员 |
| 3 | `GET` | `/api/v1/users` | 巡检员列表 | 管理员 |
| 4 | `GET` | `/api/v1/tasks` | 任务列表（分页+筛选） | 管理员/巡检员 |
| 5 | `GET` | `/api/v1/tasks/my` | 我的巡检任务（移动端） | 巡检员 |
| 6 | `GET` | `/api/v1/tasks/{id}` | 任务详情（含线路明细） | 全员 |
| 7 | `POST` | `/api/v1/tasks` | 下发任务（多线路） | 管理员 |
| 8 | `PUT` | `/api/v1/tasks/{id}/review` | 审核任务 | 管理员 |
| 9 | `PUT` | `/api/v1/task-routes/{routeId}/start` | 开始巡检某线路 | 巡检员 |
| 10 | `PUT` | `/api/v1/task-routes/{routeId}/arrive` | 标记到达巡检位置 | 巡检员 |
| 11 | `POST` | `/api/v1/task-routes/{routeId}/submit` | 提交某线路巡检结果 | 巡检员 |
| 12 | `POST` | `/api/v1/task-routes/{routeId}/tracks` | 上报单个轨迹点 | 巡检员 |
| 13 | `POST` | `/api/v1/task-routes/{routeId}/tracks/batch` | 批量上报轨迹点 | 巡检员 |
| 14 | `GET` | `/api/v1/task-routes/{routeId}/tracks` | 获取巡检轨迹（PC回放） | 管理员/巡检员 |
| 15 | `GET` | `/api/v1/task-routes/{routeId}/issues` | 获取线路巡检问题 | 全员 |
| 16 | `POST` | `/api/v1/task-routes/{routeId}/issues` | 添加巡检问题（含拍照） | 巡检员 |
| 17 | `POST` | `/api/v1/upload` | 图片上传 | 巡检员 |

---

## 十四、补充说明

1. **超时逻辑**：查询时动态判定。列表/详情接口在 SQL 层或应用层根据 deadline < now() 且线路未全部完成/驳回时映射为"已超时"。
2. **图片上传**：建议独立上传接口 `POST /api/v1/upload`，巡检员先上传图片获取 URL，再提交巡检结果。移动端拍照后即时上传。
3. **已驳回重提**：巡检员针对被驳回的线路修改问题数据后，再次调用 `POST /api/v1/task-routes/{id}/submit`，后端重置为 `AWAITING_REVIEW`。
4. **审核粒度**：按线路审核，管理员可针对单条线路通过或驳回，驳回需填写原因。
5. **进度展示**：列表返回 `routeCount` 和 `completedRouteCount`，前端展示 `2/3 条` 或进度条。
6. **地图 SDK**：移动端建议高德地图 H5 JSAPI，PC 端迷你地图同用高德 JSAPI 2.0。
7. **轨迹打点频率**：建议 3~5 秒一次，弱网时本地缓存批量上传（`/tracks/batch`）。
8. **到达判定**：GPS 距离线路起点 ≤ 100m 触发提醒，每条线路每周期仅提醒一次。
9. **离线支持**：弱网/无网时轨迹点和问题本地暂存（localStorage/IndexedDB），恢复网络后批量同步。
